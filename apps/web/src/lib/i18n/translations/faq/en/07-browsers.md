# Which browsers work?

A current Chromium, Firefox, or Safari build with WebAssembly and Web Workers. Linking a demo file so Open can reload it without re-dropping uses the File System Access API, which is Chromium-only.

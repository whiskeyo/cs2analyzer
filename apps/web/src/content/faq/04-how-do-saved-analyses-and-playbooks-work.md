# How do saved analyses and playbooks work?

Drawings auto-save in this browser. The demo itself is not stored, so *Open* asks you to drop the same `.dem` (or a linked file) to restore them. Export a JSON backup from Settings so a cache wipe does not eat them.
